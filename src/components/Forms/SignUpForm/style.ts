import { StyleSheet } from 'react-native';

export const stylesSignUpForm = StyleSheet.create({
  Container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  signupContainer: {
    alignItems: 'center',
    padding: 10,
    elevation: 10,
  },
  Field: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
});
